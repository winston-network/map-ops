{
  "name": "offline-map-app",
  "version": "1.0.0",
  "description": "Offline Map Application with custom basemap support",
  "main": "tile-server.js",
  "scripts": {
    "serve-tiles": "node tile-server.js",
    "serve-app": "python -m http.server 8000",
    "start": "echo 'Run npm run serve-tiles in one terminal and npm run serve-app in another'"
  },
  "dependencies": {
    "better-sqlite3": "^9.0.0"
  },
  "optionalDependencies": {
    "sql.js": "^1.8.0"
  }
}
